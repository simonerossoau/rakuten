import ISectionModel from "../../models/sectionModel";

export default interface IIndexState {
    sectionData: ISectionModel,
    sectionData1: ISectionModel,
    sectionData2: ISectionModel,
    sliderData: any[],
    bannersData: any[]
}